#include "main.h"

/**
 * _islower - check the code.
 *
 * @c: value to be checked
 *
 * Return: 1 when 1 if c lowercase 0 otherwise
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{	
	return (0);
	}
}
